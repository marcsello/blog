<!DOCTYPE html>
<html dir="ltr" lang="en">
<head>
    <meta charset="utf-8"/>
    <title>{% block page_title %}Marcsello's blog{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1"/>
    <link href="style.css" rel="stylesheet"/>
    <meta name="apple-mobile-web-app-title" content="{{ self.page_title() }}"/>
    <meta property="og:site_name" content="Marcsello's blog"/>
    <meta property="og:title" content="{{ self.page_title() }}"/>
    <meta property="og:url" content="{{ site.url_base }}"/>
    <meta property="article:author" content="Marcsello"/>
    <link rel="alternate" type="application/rss+xml" title="RSS Feed for Marcsello's blog" href="{{ [site.url_base, site.config.RSS_FILE_PATH]|urljoin }}">
    {% block og_extra_headers %}
        <meta property="og:type" content="website"/>
        <meta property="og:description" content="..."/>
    {% endblock %}
</head>
<body>
<div>
    <header>
        <h1>Marcsello's blog</h1>
        <h2>My very own blog</h2>
    </header>
    <main>
        {% block content required %}{% endblock %}
    </main>
</div>
</body>
</html>
